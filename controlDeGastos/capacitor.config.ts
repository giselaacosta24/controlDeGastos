import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.controlDeGastos.starter',
  appName: 'controlDeGastos',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
